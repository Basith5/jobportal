<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Job Seeker List</title>
    <style>
        body {
            background-color: #f5f5f5;
            margin: 0;
            padding: 0;
        }
        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px;
            background: linear-gradient(to right, #007bff, #00a5e0);
            color: white;
            top: 0;
            position: sticky;
            z-index: 1;
        }

    nav button {
        background-color: red;
        color: white;
        border: none;
        padding: 10px 20px;
        border-radius: 5px;
        font-size: 16px;
        cursor: pointer;
    }

    .job-seeker-list {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 20px;
        margin: 30px;
    }

    .job-seeker-card {
        background-color: aliceblue;
        padding: 20px;
        border-radius: 10px;
        color: black;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        position: relative;
    }

    .profile-circle {
        width: 80px;
        height: 80px;
        background-color: lightblue;
        border-radius: 50%;
        display: flex;
        justify-content: center;
        align-items: center;
        margin-bottom: 10px;
        font-size: 24px;
        font-weight: bold;
    }

    .job-seeker-card h3, .job-seeker-card h4, .job-seeker-card p {
        margin: 5px 0;
    }

    .job-seeker-card h3 {
        font-size: 18px;
        font-weight: bold;
    }

    .job-seeker-card h4 {
        font-size: 14px;
        font-weight: normal;
    }

    .job-seeker-card p {
        font-size: 14px;
    }

    .aname{
        color: #007bff;
        font-weight: bold;
        text-transform: uppercase;
    }

    .ajob{
        color: red;
        font-weight: bold;
    }

    .hire-button {
        background-color: rgb(40, 235, 40);
        color: white;
        border: none;
        padding: 8px 12px;
        border-radius: 5px;
        font-size: 14px;
        position: absolute;
        bottom: 10px;
        right: 10px;
        cursor: pointer;
        width: 60px;
    }
    span{
        color: green;
        font-weight: bold;
        font-size: 15px;
    }
</style>
</head>
<body>
    <nav>
        <h1>JOB SEEKER LIST</h1>
        <button>Logout</button>
    </nav>
    <div class="job-seeker-list">
        {{#each rows}}
        <div class="job-seeker-card"> 
            <div class="profile-circle">AB</div>
                <h3 class="aname">{{this.name}}</h3>
                <p>{{this.gender}}</p>
                <p>{{this.email}}</p>
                <p class="ajob">{{this.job}}</p>
                <p><span>{{this.experience}}</span> Experience</p>
                <button class="hire-button">Hire</button>
        </div>
        {{/each}}
    </div> 
</body>
</html>